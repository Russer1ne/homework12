const obj = Object.create(null)

// не совсем понял задание. но из информации что я прочитал пустой обьект создается так))

-----

Всё верно, только по заданию создание объекта без прототипа нужно обернуть в функцию :) вот так:

function createObjWithoutProto() {
  return Object.create(null);
}
